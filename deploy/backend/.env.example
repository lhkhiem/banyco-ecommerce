# Backend Environment Variables Example
# Copy this file to .env.local for development

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3012
API_PORT=3012
FRONTEND_PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=spa_cms_user
DB_NAME=banyco
DB_PASSWORD=your_local_db_password

# JWT Secrets (generate strong secrets - minimum 32 characters)
JWT_SECRET=your_local_jwt_secret_minimum_32_characters
JWT_REFRESH_SECRET=your_local_jwt_refresh_secret_minimum_32_characters

# CORS Configuration
ECOMMERCE_FRONTEND_ORIGIN=http://localhost:3000
FRONTEND_DOMAIN=localhost:3000

#  Domain Configuration (all configurable via env - no hardcoded domains)
API_DOMAIN=localhost:3012
PRODUCTION_API_DOMAIN=ecommerce-api.banyco.vn
PRODUCTION_FRONTEND_DOMAIN=banyco.vn
PRODUCTION_DOMAIN_SUFFIX=banyco.vn

# CMS Backend Configuration
CMS_BACKEND_URL=http://localhost:3011
CMS_BACKEND_PORT=3011
CMS_API_DOMAIN=api.banyco.vn

#  Image Source URL
# This is where images are stored/served from
# - Development: CMS backend (localhost:3011)
# - Production: Can be CMS backend or S3 URL
# - Future: Change to S3 URL (e.g., https://s3.amazonaws.com/bucket) for upgrade
IMAGE_SOURCE_URL=http://localhost:3011
